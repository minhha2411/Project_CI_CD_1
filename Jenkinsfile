pipeline {
    agent any {
        stages('Build') {
            steps {
                echo "Start building stage ...."
            }
        }
        stages('Test') {
            steps {
                echo "Start testing stage ...."
            }
        }
        stages('Deploy') {
            steps {
                echo "Start deploy stage ...."
            }
        }
    }
}